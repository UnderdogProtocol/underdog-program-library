/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  AccountMeta,
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findInitialOwnerPda, findOrgAccountPda } from '../accounts';
import { addAccountMeta, addObjectProperty } from '../shared';

// Accounts.
export type InitializeOrgV1InstructionAccounts = {
  authority?: Signer;
  ownerAccount?: PublicKey | Pda;
  orgAccount?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
  rent?: PublicKey | Pda;
};

// Data.
export type InitializeOrgV1InstructionData = {
  discriminator: Array<number>;
  superAdminAddress: PublicKey;
  orgId: string;
};

export type InitializeOrgV1InstructionDataArgs = {
  superAdminAddress: PublicKey;
  orgId: string;
};

/** @deprecated Use `getInitializeOrgV1InstructionDataSerializer()` without any argument instead. */
export function getInitializeOrgV1InstructionDataSerializer(
  _context: object
): Serializer<
  InitializeOrgV1InstructionDataArgs,
  InitializeOrgV1InstructionData
>;
export function getInitializeOrgV1InstructionDataSerializer(): Serializer<
  InitializeOrgV1InstructionDataArgs,
  InitializeOrgV1InstructionData
>;
export function getInitializeOrgV1InstructionDataSerializer(
  _context: object = {}
): Serializer<
  InitializeOrgV1InstructionDataArgs,
  InitializeOrgV1InstructionData
> {
  return mapSerializer<
    InitializeOrgV1InstructionDataArgs,
    any,
    InitializeOrgV1InstructionData
  >(
    struct<InitializeOrgV1InstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['superAdminAddress', publicKeySerializer()],
        ['orgId', string()],
      ],
      { description: 'InitializeOrgV1InstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [61, 209, 144, 30, 101, 120, 165, 185],
    })
  ) as Serializer<
    InitializeOrgV1InstructionDataArgs,
    InitializeOrgV1InstructionData
  >;
}

// Args.
export type InitializeOrgV1InstructionArgs = InitializeOrgV1InstructionDataArgs;

// Instruction.
export function initializeOrgV1(
  context: Pick<Context, 'programs' | 'eddsa' | 'identity'>,
  input: InitializeOrgV1InstructionAccounts & InitializeOrgV1InstructionArgs
): TransactionBuilder {
  const signers: Signer[] = [];
  const keys: AccountMeta[] = [];

  // Program ID.
  const programId = context.programs.getPublicKey(
    'underdogCore',
    'updg8JyjrmFE2h3d71p71zRXDR8q4C6Up8dDoeq3LTM'
  );

  // Resolved inputs.
  const resolvedAccounts = {};
  const resolvingArgs = {};
  addObjectProperty(
    resolvedAccounts,
    'authority',
    input.authority
      ? ([input.authority, true] as const)
      : ([context.identity, true] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'ownerAccount',
    input.ownerAccount
      ? ([input.ownerAccount, false] as const)
      : ([findInitialOwnerPda(context), false] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'orgAccount',
    input.orgAccount
      ? ([input.orgAccount, true] as const)
      : ([
          findOrgAccountPda(context, {
            superAdminAddress: input.superAdminAddress,
            orgId: input.orgId,
          }),
          true,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'systemProgram',
    input.systemProgram
      ? ([input.systemProgram, false] as const)
      : ([
          context.programs.getPublicKey(
            'splSystem',
            '11111111111111111111111111111111'
          ),
          false,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'rent',
    input.rent
      ? ([input.rent, false] as const)
      : ([
          publicKey('SysvarRent111111111111111111111111111111111'),
          false,
        ] as const)
  );
  const resolvedArgs = { ...input, ...resolvingArgs };

  addAccountMeta(keys, signers, resolvedAccounts.authority, false);
  addAccountMeta(keys, signers, resolvedAccounts.ownerAccount, false);
  addAccountMeta(keys, signers, resolvedAccounts.orgAccount, false);
  addAccountMeta(keys, signers, resolvedAccounts.systemProgram, false);
  addAccountMeta(keys, signers, resolvedAccounts.rent, false);

  // Data.
  const data =
    getInitializeOrgV1InstructionDataSerializer().serialize(resolvedArgs);

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
