/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  AccountMeta,
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { addAccountMeta, addObjectProperty } from '../shared';

// Accounts.
export type InitializeOwnerInstructionAccounts = {
  authority?: Signer;
  ownerAccount: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializeOwnerInstructionData = { discriminator: Array<number> };

export type InitializeOwnerInstructionDataArgs = {};

/** @deprecated Use `getInitializeOwnerInstructionDataSerializer()` without any argument instead. */
export function getInitializeOwnerInstructionDataSerializer(
  _context: object
): Serializer<
  InitializeOwnerInstructionDataArgs,
  InitializeOwnerInstructionData
>;
export function getInitializeOwnerInstructionDataSerializer(): Serializer<
  InitializeOwnerInstructionDataArgs,
  InitializeOwnerInstructionData
>;
export function getInitializeOwnerInstructionDataSerializer(
  _context: object = {}
): Serializer<
  InitializeOwnerInstructionDataArgs,
  InitializeOwnerInstructionData
> {
  return mapSerializer<
    InitializeOwnerInstructionDataArgs,
    any,
    InitializeOwnerInstructionData
  >(
    struct<InitializeOwnerInstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'InitializeOwnerInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [106, 243, 135, 113, 73, 21, 241, 19],
    })
  ) as Serializer<
    InitializeOwnerInstructionDataArgs,
    InitializeOwnerInstructionData
  >;
}

// Instruction.
export function initializeOwner(
  context: Pick<Context, 'programs' | 'identity'>,
  input: InitializeOwnerInstructionAccounts
): TransactionBuilder {
  const signers: Signer[] = [];
  const keys: AccountMeta[] = [];

  // Program ID.
  const programId = context.programs.getPublicKey(
    'underdogCore',
    'updg8JyjrmFE2h3d71p71zRXDR8q4C6Up8dDoeq3LTM'
  );

  // Resolved inputs.
  const resolvedAccounts = {
    ownerAccount: [input.ownerAccount, true] as const,
  };
  addObjectProperty(
    resolvedAccounts,
    'authority',
    input.authority
      ? ([input.authority, true] as const)
      : ([context.identity, true] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'systemProgram',
    input.systemProgram
      ? ([input.systemProgram, false] as const)
      : ([
          context.programs.getPublicKey(
            'splSystem',
            '11111111111111111111111111111111'
          ),
          false,
        ] as const)
  );

  addAccountMeta(keys, signers, resolvedAccounts.authority, false);
  addAccountMeta(keys, signers, resolvedAccounts.ownerAccount, false);
  addAccountMeta(keys, signers, resolvedAccounts.systemProgram, false);

  // Data.
  const data = getInitializeOwnerInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
