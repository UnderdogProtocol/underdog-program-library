/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  AccountMeta,
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { addAccountMeta, addObjectProperty } from '../shared';

// Accounts.
export type WithdrawProjectRoyaltiesV0InstructionAccounts = {
  authority?: Signer;
  ownerAccount: PublicKey | Pda;
  orgAccount: PublicKey | Pda;
  projectAccount: PublicKey | Pda;
  destination: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type WithdrawProjectRoyaltiesV0InstructionData = {
  discriminator: Array<number>;
  superAdminAddress: PublicKey;
  orgId: string;
  projectId: bigint;
};

export type WithdrawProjectRoyaltiesV0InstructionDataArgs = {
  superAdminAddress: PublicKey;
  orgId: string;
  projectId: number | bigint;
};

/** @deprecated Use `getWithdrawProjectRoyaltiesV0InstructionDataSerializer()` without any argument instead. */
export function getWithdrawProjectRoyaltiesV0InstructionDataSerializer(
  _context: object
): Serializer<
  WithdrawProjectRoyaltiesV0InstructionDataArgs,
  WithdrawProjectRoyaltiesV0InstructionData
>;
export function getWithdrawProjectRoyaltiesV0InstructionDataSerializer(): Serializer<
  WithdrawProjectRoyaltiesV0InstructionDataArgs,
  WithdrawProjectRoyaltiesV0InstructionData
>;
export function getWithdrawProjectRoyaltiesV0InstructionDataSerializer(
  _context: object = {}
): Serializer<
  WithdrawProjectRoyaltiesV0InstructionDataArgs,
  WithdrawProjectRoyaltiesV0InstructionData
> {
  return mapSerializer<
    WithdrawProjectRoyaltiesV0InstructionDataArgs,
    any,
    WithdrawProjectRoyaltiesV0InstructionData
  >(
    struct<WithdrawProjectRoyaltiesV0InstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['superAdminAddress', publicKeySerializer()],
        ['orgId', string()],
        ['projectId', u64()],
      ],
      { description: 'WithdrawProjectRoyaltiesV0InstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [124, 137, 160, 171, 88, 68, 93, 233],
    })
  ) as Serializer<
    WithdrawProjectRoyaltiesV0InstructionDataArgs,
    WithdrawProjectRoyaltiesV0InstructionData
  >;
}

// Args.
export type WithdrawProjectRoyaltiesV0InstructionArgs =
  WithdrawProjectRoyaltiesV0InstructionDataArgs;

// Instruction.
export function withdrawProjectRoyaltiesV0(
  context: Pick<Context, 'programs' | 'identity'>,
  input: WithdrawProjectRoyaltiesV0InstructionAccounts &
    WithdrawProjectRoyaltiesV0InstructionArgs
): TransactionBuilder {
  const signers: Signer[] = [];
  const keys: AccountMeta[] = [];

  // Program ID.
  const programId = context.programs.getPublicKey(
    'underdogCore',
    'updg8JyjrmFE2h3d71p71zRXDR8q4C6Up8dDoeq3LTM'
  );

  // Resolved inputs.
  const resolvedAccounts = {
    ownerAccount: [input.ownerAccount, true] as const,
    orgAccount: [input.orgAccount, false] as const,
    projectAccount: [input.projectAccount, true] as const,
    destination: [input.destination, true] as const,
  };
  const resolvingArgs = {};
  addObjectProperty(
    resolvedAccounts,
    'authority',
    input.authority
      ? ([input.authority, false] as const)
      : ([context.identity, false] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'systemProgram',
    input.systemProgram
      ? ([input.systemProgram, false] as const)
      : ([
          context.programs.getPublicKey(
            'splSystem',
            '11111111111111111111111111111111'
          ),
          false,
        ] as const)
  );
  const resolvedArgs = { ...input, ...resolvingArgs };

  addAccountMeta(keys, signers, resolvedAccounts.authority, false);
  addAccountMeta(keys, signers, resolvedAccounts.ownerAccount, false);
  addAccountMeta(keys, signers, resolvedAccounts.orgAccount, false);
  addAccountMeta(keys, signers, resolvedAccounts.projectAccount, false);
  addAccountMeta(keys, signers, resolvedAccounts.destination, false);
  addAccountMeta(keys, signers, resolvedAccounts.systemProgram, false);

  // Data.
  const data =
    getWithdrawProjectRoyaltiesV0InstructionDataSerializer().serialize(
      resolvedArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
