/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  findMasterEditionPda,
  findMetadataPda,
} from '@metaplex-foundation/mpl-token-metadata';
import {
  AccountMeta,
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  findLegacyProjectPda,
  findOrgAccountPda,
  findOrgControlAccountPda,
  findOrgMemberAccountPda,
} from '../accounts';
import { addAccountMeta, addObjectProperty } from '../shared';

// Accounts.
export type InitializeCompressedProjectInstructionAccounts = {
  authority?: Signer;
  orgAccount?: PublicKey | Pda;
  orgControlAccount?: PublicKey | Pda;
  memberAccount?: PublicKey | Pda;
  compressedProject?: PublicKey | Pda;
  compressedProjectMint?: PublicKey | Pda;
  compressedProjectVault?: PublicKey | Pda;
  compressedProjectMetadata?: PublicKey | Pda;
  compressedProjectMasterEdition?: PublicKey | Pda;
  tokenMetadataProgram?: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
  rent?: PublicKey | Pda;
};

// Data.
export type InitializeCompressedProjectInstructionData = {
  discriminator: Array<number>;
  superAdminAddress: PublicKey;
  memberAddress: PublicKey;
  orgId: string;
  projectIdStr: string;
  name: string;
  symbol: string;
  uri: string;
};

export type InitializeCompressedProjectInstructionDataArgs = {
  superAdminAddress: PublicKey;
  memberAddress: PublicKey;
  orgId: string;
  projectIdStr: string;
  name: string;
  symbol: string;
  uri: string;
};

/** @deprecated Use `getInitializeCompressedProjectInstructionDataSerializer()` without any argument instead. */
export function getInitializeCompressedProjectInstructionDataSerializer(
  _context: object
): Serializer<
  InitializeCompressedProjectInstructionDataArgs,
  InitializeCompressedProjectInstructionData
>;
export function getInitializeCompressedProjectInstructionDataSerializer(): Serializer<
  InitializeCompressedProjectInstructionDataArgs,
  InitializeCompressedProjectInstructionData
>;
export function getInitializeCompressedProjectInstructionDataSerializer(
  _context: object = {}
): Serializer<
  InitializeCompressedProjectInstructionDataArgs,
  InitializeCompressedProjectInstructionData
> {
  return mapSerializer<
    InitializeCompressedProjectInstructionDataArgs,
    any,
    InitializeCompressedProjectInstructionData
  >(
    struct<InitializeCompressedProjectInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['superAdminAddress', publicKeySerializer()],
        ['memberAddress', publicKeySerializer()],
        ['orgId', string()],
        ['projectIdStr', string()],
        ['name', string()],
        ['symbol', string()],
        ['uri', string()],
      ],
      { description: 'InitializeCompressedProjectInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [189, 42, 65, 147, 175, 216, 254, 22],
    })
  ) as Serializer<
    InitializeCompressedProjectInstructionDataArgs,
    InitializeCompressedProjectInstructionData
  >;
}

// Args.
export type InitializeCompressedProjectInstructionArgs =
  InitializeCompressedProjectInstructionDataArgs;

// Instruction.
export function initializeCompressedProject(
  context: Pick<Context, 'programs' | 'eddsa' | 'identity'>,
  input: InitializeCompressedProjectInstructionAccounts &
    InitializeCompressedProjectInstructionArgs
): TransactionBuilder {
  const signers: Signer[] = [];
  const keys: AccountMeta[] = [];

  // Program ID.
  const programId = context.programs.getPublicKey(
    'underdogCore',
    'updg8JyjrmFE2h3d71p71zRXDR8q4C6Up8dDoeq3LTM'
  );

  // Resolved inputs.
  const resolvedAccounts = {};
  const resolvingArgs = {};
  addObjectProperty(
    resolvedAccounts,
    'authority',
    input.authority
      ? ([input.authority, true] as const)
      : ([context.identity, true] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'orgAccount',
    input.orgAccount
      ? ([input.orgAccount, true] as const)
      : ([
          findOrgAccountPda(context, {
            superAdminAddress: input.superAdminAddress,
            orgId: input.orgId,
          }),
          true,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'orgControlAccount',
    input.orgControlAccount
      ? ([input.orgControlAccount, true] as const)
      : ([
          findOrgControlAccountPda(context, {
            superAdminAddress: input.superAdminAddress,
            orgId: input.orgId,
          }),
          true,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'memberAccount',
    input.memberAccount
      ? ([input.memberAccount, true] as const)
      : ([
          findOrgMemberAccountPda(context, {
            orgAccount: publicKey(resolvedAccounts.orgAccount[0], false),
            member: input.memberAddress,
          }),
          true,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'compressedProject',
    input.compressedProject
      ? ([input.compressedProject, true] as const)
      : ([
          findLegacyProjectPda(context, {
            type: 'c-proj',
            orgAccount: publicKey(resolvedAccounts.orgAccount[0], false),
            projectId: input.projectIdStr,
          }),
          true,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'compressedProjectMint',
    input.compressedProjectMint
      ? ([input.compressedProjectMint, true] as const)
      : ([
          findLegacyProjectPda(context, {
            type: 'c-project-mint',
            orgAccount: publicKey(resolvedAccounts.orgAccount[0], false),
            projectId: input.projectIdStr,
          }),
          true,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'compressedProjectVault',
    input.compressedProjectVault
      ? ([input.compressedProjectVault, true] as const)
      : ([
          findLegacyProjectPda(context, {
            type: 'c-project-vault',
            orgAccount: publicKey(resolvedAccounts.orgAccount[0], false),
            projectId: input.projectIdStr,
          }),
          true,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'compressedProjectMetadata',
    input.compressedProjectMetadata
      ? ([input.compressedProjectMetadata, true] as const)
      : ([
          findMetadataPda(context, {
            mint: publicKey(resolvedAccounts.compressedProjectMint[0], false),
          }),
          true,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'compressedProjectMasterEdition',
    input.compressedProjectMasterEdition
      ? ([input.compressedProjectMasterEdition, true] as const)
      : ([
          findMasterEditionPda(context, {
            mint: publicKey(resolvedAccounts.compressedProjectMint[0], false),
          }),
          true,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'tokenMetadataProgram',
    input.tokenMetadataProgram
      ? ([input.tokenMetadataProgram, false] as const)
      : ([
          context.programs.getPublicKey(
            'mplTokenMetadata',
            'metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s'
          ),
          false,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'tokenProgram',
    input.tokenProgram
      ? ([input.tokenProgram, false] as const)
      : ([
          context.programs.getPublicKey(
            'splToken',
            'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
          ),
          false,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'systemProgram',
    input.systemProgram
      ? ([input.systemProgram, false] as const)
      : ([
          context.programs.getPublicKey(
            'splSystem',
            '11111111111111111111111111111111'
          ),
          false,
        ] as const)
  );
  addObjectProperty(
    resolvedAccounts,
    'rent',
    input.rent
      ? ([input.rent, false] as const)
      : ([
          publicKey('SysvarRent111111111111111111111111111111111'),
          false,
        ] as const)
  );
  const resolvedArgs = { ...input, ...resolvingArgs };

  addAccountMeta(keys, signers, resolvedAccounts.authority, false);
  addAccountMeta(keys, signers, resolvedAccounts.orgAccount, false);
  addAccountMeta(keys, signers, resolvedAccounts.orgControlAccount, false);
  addAccountMeta(keys, signers, resolvedAccounts.memberAccount, false);
  addAccountMeta(keys, signers, resolvedAccounts.compressedProject, false);
  addAccountMeta(keys, signers, resolvedAccounts.compressedProjectMint, false);
  addAccountMeta(keys, signers, resolvedAccounts.compressedProjectVault, false);
  addAccountMeta(
    keys,
    signers,
    resolvedAccounts.compressedProjectMetadata,
    false
  );
  addAccountMeta(
    keys,
    signers,
    resolvedAccounts.compressedProjectMasterEdition,
    false
  );
  addAccountMeta(keys, signers, resolvedAccounts.tokenMetadataProgram, false);
  addAccountMeta(keys, signers, resolvedAccounts.tokenProgram, false);
  addAccountMeta(keys, signers, resolvedAccounts.systemProgram, false);
  addAccountMeta(keys, signers, resolvedAccounts.rent, false);

  // Data.
  const data =
    getInitializeCompressedProjectInstructionDataSerializer().serialize(
      resolvedArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
